/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you had any specific configurations, like reactStrictMode: true,
  // make sure to copy them here inside this object.
  // If it was empty before, you can leave this empty too.
};

module.exports = nextConfig;